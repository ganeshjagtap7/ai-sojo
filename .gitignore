# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# startupage scraper (local session + generated output)
startupage-auth.json
startupage-leads.xlsx
startupage-*.html

# apppeak scraper (generated output + inspection dumps)
apppeak-leads.xlsx
apppeak-*.html

# quietlight scraper (generated output + inspection dumps)
quietlight-leads.xlsx
quietlight-*.html

# websiteclosers scraper (generated output + inspection dumps)
websiteclosers-leads.xlsx
websiteclosers-*.html

# investors club scraper (generated output + inspection dumps)
investorsclub-leads.xlsx
investorsclub-*.html

# motion invest scraper (local session + generated output + inspection dumps)
motioninvest-auth.json
motioninvest-leads.xlsx
motioninvest-*.html

# microns scraper (local session + generated output + inspection dumps)
microns-auth.json
microns-leads.xlsx
microns-*.html
microns-api-*.json

# synergy business brokers scraper (generated output + inspection dumps)
synergy-leads.xlsx
synergy-*.html

# synergy business brokers scraper (generated output + inspection dumps)
synergy-leads.xlsx
synergy-*.html
