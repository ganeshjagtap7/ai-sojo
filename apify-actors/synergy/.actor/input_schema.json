{
  "title": "Synergy Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "maxItems": {
      "title": "Max items",
      "type": "integer",
      "description": "Maximum number of listings to return (also caps scroll + detail fetches).",
      "default": 50,
      "minimum": 1,
      "editor": "number"
    }
  }
}
