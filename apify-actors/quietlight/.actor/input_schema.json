{
  "title": "Quiet Light Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "maxItems": {
      "title": "Max items",
      "type": "integer",
      "description": "Maximum number of listings to return.",
      "default": 50,
      "minimum": 1,
      "editor": "number"
    },
    "residential": {
      "title": "Use residential proxy (paid)",
      "type": "boolean",
      "description": "Datacenter proxy is used by default (free). Turn on to use residential proxy if Cloudflare blocks the datacenter IP (paid add-on).",
      "default": false
    }
  }
}
